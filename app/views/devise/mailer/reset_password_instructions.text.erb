<% front_end_url = ENV.fetch("FRONTEND_URL", "http://localhost:5173") %>
Hello <%= @resource.first_name.presence || @resource.email %>,

We received a request to reset the password for your Page Chat account.

Reset your password using the link below:
<%= "#{front_end_url}/reset-password?token=#{@token}&email=#{ERB::Util.url_encode(@resource.email)}" %>

If you didn’t request this, you can ignore this message and your password will stay the same.

— The Page Chat Team

